<%= form_for(trivia_question) do |f| %>
  <% if trivia_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trivia_question.errors.count, "error") %> prohibited this trivia_question from being saved:</h2>

      <ul>
      <% trivia_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_area :question %>
  </div>

  <div class="field">
    <%= f.label :tag_list %>
    <%= f.text_area :tag_list %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
